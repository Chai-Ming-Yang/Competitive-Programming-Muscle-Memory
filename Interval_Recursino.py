seg = [[] for _ in range(n*4)]

def add(node, nl, nr, l, r, item):
  if disjoint: return
  if fully covered: store
  else: split

def dfs(node, nl, nr):
  save state rollback
  enter node → apply changes
  recurse → children
  exit node → rollback
